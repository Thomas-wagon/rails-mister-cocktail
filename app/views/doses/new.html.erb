<h1> <%= @cocktail.name %> </h1>
<%= simple_form_for [ @cocktail, @dose ] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %>
  <%= f.submit "Add this ingrdient !", class: "btn btn-primary" %>
<% end %>